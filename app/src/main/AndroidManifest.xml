<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="puzzle.child.gams">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- <supports-screens -->
    <!-- android:anyDensity="true" -->
    <!-- android:largeScreens="true" -->
    <!-- android:normalScreens="true" -->
    <!-- android:smallScreens="false" -->
    <!-- android:xlargeScreens="true" /> -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test.MainActivity">

        </activity>
        <activity
            android:name=".test.SlidePuzzleMain"
            android:label="@string/title_activity_slide_puzzle_main" />
        <activity android:name=".test.MenuActivity">


        </activity>
        <activity android:name=".gamePuzzle.BaseActivity" />
        <activity
            android:name=".gamePuzzle.YourImageActivity"
            android:theme="@style/Theme" />
        <activity
            android:name=".gamePuzzle.ImageFromYourGalayActivity"
            android:theme="@style/Theme" />
        <activity android:name=".user.SplachScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.LoginActivity">


        </activity>
        <activity android:name=".user.RegisterAccountActivity" />
        <activity android:name=".user.UserAcount" />
        <activity android:name=".ui.CircleMenuActivity"></activity>
        <activity android:name=".ui.CircleActivity"></activity>
        <activity
            android:name=".ui.MainCircleActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme">

        </activity>
        <activity android:name=".user.MenuHisImage"></activity>
    </application>

</manifest>